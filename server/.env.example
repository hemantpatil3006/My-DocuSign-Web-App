PORT=5000
MONGO_URI=
JWT_SECRET=
JWT_REFRESH_SECRET=

# Used to generate public invitation/reset links
FRONTEND_URL=http://localhost:5173

# Email (SMTP) - required for invitation/reset emails
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=

# Optional (used by forgot-password flow)
EMAIL_SERVICE=gmail
